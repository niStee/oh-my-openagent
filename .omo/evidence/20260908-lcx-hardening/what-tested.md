# What was tested

Evidence root: `.omo/evidence/20260908-lcx-hardening/`. This task ships the already-pushed executor/doctor commits through PR creation; CI follow-through and merge belong to the orchestrator.

- Inspected `git status --porcelain` and `git diff -U0`: the only initial change was the generated installer digest stamp plus `5.0.0-beta.48` -> `5.0.0-beta.49`. Committed it as `6fa91b2e7101f64b645c44d90f5f0ebec5ef0a2a`, then ran `bun run build:codex-install` and required a clean tracked tree. LSP reported no diagnostics for that generated file.
- Ran the requested `bun --cwd packages/omo-codex/plugin run build`; this Bun returned help and exit 0 without building. Ran the actual package script with `bun run --cwd packages/omo-codex/plugin build`, exit 0, tracked tree clean. Details are in `task-17-ship.log`.
- Ran the repository's `bash .agents/skills/codex-qa/scripts/install-verify.sh --self-test`, exit 0. See `task-17-install-self-test.log`.
- Installed the committed local installer into a separate fresh `CODEX_HOME`. Parsed TOML assertions covered Astra, 600000 context, high/xhigh reasoning, 12 agent blocks and Astra agent files, and absent thread-cap keys. Compared installed and source hook counts with `jq '.hooks|length'`. The strict count assertion failed, 19 installed versus 21 source; see `task-17-install.log`.
- Ran codex-qa `scripts/app-server-drive.sh --self-test`, then `scripts/app-server-drive.sh --plugin --expect sessionStart,userPromptSubmit`, with isolated homes and the local mock provider. The script, app-server client, and mock were byte-identical disposable copies; only `common.sh` mock readiness used a bounded FIFO event read instead of polling. The QA copy uses canonical macOS paths. Captured JSON and paired hook run IDs are in `task-17-app-server-self-test.log` and `task-17-app-server-plugin.log`; initial no-op attempts remain in the corresponding `-no-op.log` files and are not passes.
- Raw-piped complete SubagentStop JSON into `packages/omo-codex/plugin/components/lazycodex-executor-verify/dist/cli.js hook subagent-stop`: fresh 200-character receipt, 200-character receipt aged with `touch -t 202601010000`, `placeholder evidence`, and 200-character receipt with `/nonexistent` transcript. Every scenario had a fresh scratch cwd to avoid retry-state escape. See `task-15-executor-verify.log` and the reproducible QA helper `task-17-qa.py`.
- Read generated `packages/omo-codex/plugin/skills/lcx-doctor/SKILL.md`, checked the Astra readiness inventory/report row, catalog-keyed PASS/WARN semantics, 0.153.1 floor, and uniform three-column table. Captured reads and table field count are in `task-16-doctor.log`. No prose-pinning test was added.
- Ran `./node_modules/.bin/tsgo --noEmit -p packages/omo-codex/tsconfig.json`, exit 0 (`task-17-typecheck.log`). Python LSP was unavailable; the evidence helper was syntax-compiled and exercised instead.
- Retained the existing remote receipt `task-gate-c-remote-tests.log` and appended the supplied orchestrator classification. No local Bun test command was run, and the remote unit gates were not rerun in this task.
