bun test v1.4.2 (744846f84)

packages/memory-core/src/reflection/worktree.test.ts:
(pass) reflection worktree finalization > #given recorded identity before Git creation #when cleanup runs #then missing resources are idempotently confirmed absent [215.87ms]
(pass) reflection worktree finalization > #given a precreate callback #when a worktree is created #then identity is published before Git resources exist [199.96ms]
(pass) reflection worktree finalization > #given a clean committed reflection #when auto-finalized #then it no-ff merges and cleans up [449.45ms]
(pass) reflection worktree finalization > #given a document-maintenance allowlist #when the child changes another path #then nothing is merged [423.37ms]
(pass) reflection worktree finalization > #given a reflection with no commits #when finalized #then it reports no_changes and cleans up [273.66ms]
(pass) reflection worktree finalization > #given conflicting child and parent commits #when auto-finalized #then merge aborts cleanly and cleanup always runs [512.23ms]
(pass) reflection worktree finalization > #given a dirty parent #when auto-finalized #then it refuses before merge and cleans up [393.34ms]
(pass) reflection worktree finalization > #given a non-conflicting parent advance after launch #when auto-finalized #then recorded-base drift merges successfully [503.28ms]
(pass) reflection worktree finalization > #given a branch deletion that keeps failing #when a merged reflection finalizes #then the landed outcome survives the incomplete cleanup [402.98ms]
(pass) reflection worktree finalization > #given an externally merged reflection branch #when explicit finalization verifies reachability #then it reports merged and cleans up [398.05ms]

 10 pass
 0 fail
 42 expect() calls
Ran 10 tests across 1 file. [4.22s]
