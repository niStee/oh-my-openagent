# GREEN run on a designated ARM64 compute host, worktree at f5dae921a1b66d65100e4da249363ff32e6fbfc8
$ bun test script/tracked-ignored-paths-audit.test.ts packages/omo-senpi/scripts/qa/out-dir-arg.test.ts
f5dae921a
--- status:
 M packages/omo-codex/scripts/install-dist/install-local.mjs
?? --report-dir/
--- tests

packages/omo-senpi/scripts/qa/out-dir-arg.test.ts:
(pass) resolveOutDirArg > #given no argument #when resolved #then the fallback directory is used [0.10ms]
(pass) resolveOutDirArg > #given a bare directory #when resolved #then that directory is used [0.06ms]
(pass) resolveOutDirArg > #given the --out-dir flag with a value #when resolved #then the value is used, not the flag name [0.05ms]
(pass) resolveOutDirArg > #given the --out-dir flag without a value #when resolved #then it is refused [0.12ms]
(pass) resolveOutDirArg > #given the --out-dir flag followed by another flag #when resolved #then it is refused [0.06ms]
(pass) resolveOutDirArg > #given an unknown flag #when resolved #then it is refused instead of becoming a directory name [0.08ms]
(pass) resolveOutDirArg > #given more than one directory #when resolved #then the extra arguments are refused [0.06ms]

 8 pass
 0 fail
 9 expect() calls
Ran 8 tests across 2 files. [389.00ms]
TEST_EXIT=0


# SURFACE: flag-like out-dir is refused, nothing is created
## bare --out-dir
13 | export function resolveOutDirArg(argv: readonly string[], fallback: string): string {
14 |   const [first, second, ...rest] = argv
15 |   if (first === undefined) return fallback
16 |   if (first === OUT_DIR_FLAG) {
17 |     if (second === undefined || second.startsWith("-")) {
18 |       throw new OutDirArgError(`${OUT_DIR_FLAG} requires a directory value`, argv)
                 ^
OutDirArgError: --out-dir requires a directory value (usage: <driver> [<out-dir> | --out-dir <out-dir>])
EXIT=1
NO_OUT_DIR_CREATED
## unknown flag
18 |       throw new OutDirArgError(`${OUT_DIR_FLAG} requires a directory value`, argv)
19 |     }
20 |     if (rest.length > 0) throw new OutDirArgError(`unexpected extra argument(s): ${rest.join(" ")}`, argv)
21 |     return second
22 |   }
EXIT=1
NO_TMP_X
