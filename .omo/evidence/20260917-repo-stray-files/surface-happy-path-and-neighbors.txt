# On a designated ARM64 compute host at f5dae921a
## happy path --out-dir /tmp/dwd-qa-1789612360
EXIT=0
dag-wait-detach-qa.json
{
  "detached_text": "Detached from dag run run-wait-detach (running, 0/2 nodes complete). The session is woken as each node completes and again when the run settles; use action=snapshot for a midpoint peek or action=wait with detach=false to block until settle.",
  "wake_delivery": {
    "customTyp
cleanup: rm -rf /tmp/dwd-qa-1789612360
NO_STRAY_DIRS
## neighbor tests
(pass) markdown local link audit > #given checked-in markdown #when maintainer-local paths are audited #then none are present [49.00ms]

 58 pass
 0 fail
 132 expect() calls
Ran 58 tests across 3 files. [827.00ms]
TEST_EXIT=0
