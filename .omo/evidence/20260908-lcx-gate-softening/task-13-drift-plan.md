# Task 13 generated-skill drift repair

## Observed baseline

- Binding contract: `/Users/yeongyu/sisyphuslabs/.omo/plans/lazycodex-astra-gate-softening.md`, todo 13.
- Worktree HEAD at inspection: eeeb3287e3af75148ecd3318800610a5e814af4a.
- The required `bun scripts/sync-skills.mjs` regeneration exited 0. Both tracked copies (init-deep/SKILL.md and ulw-plan/SKILL.md) already match regenerated output; neither has a diff. init-deep was regenerated by existing commit adb92840f.
- The targeted local Node suite still fails with `review-work drifted from shared-skills`, matching the remote failure log.
- Contrary to the task's initial diagnosis, sync-skills.test.mjs uses removeCodexSkillOverlays from sync-skills-test-support.mjs, which duplicates the previous mandatory-review prose and old ulw-execute anchors. The generator's anchors and output already implement todo 13. Changing the generator back to satisfy that inverse helper would violate the self-review contract.
- An independent worker has modified components/ulw-loop/package.json to bundle its CLI. Preserve and overlay that change for the aggregate remote run; do not edit or claim ownership of it.

## Decision

Regeneration-only was attempted and cannot fix the stale inverse comparison. Choose forward artifact equality: compare generated bytes with the shared source transformed through the current production Codex overlay. This preserves the exact-copy assertion without duplicating authored prose in a test helper or reverting approved behavior. This necessarily updates the comparison rather than the already-correct generator; no production behavior change is needed.

## Execution / verification units

1. Update both existing drift assertions in plugin/test/sync-skills.test.mjs to apply production overlays to expected source bytes rather than stripping old overlays from actual bytes. Remove the now-unused inverse overlay implementation from plugin/test/sync-skills-test-support.mjs. Keep compatibility removal, file inventories, and all test cases.
2. Run diagnostics on both changed JavaScript files before build, plus syntax checks. Run the permitted local `node --test test/sync-skills.test.mjs` and `node --test test/sync-skills-codex-compatibility.test.mjs` file inspections. No local Bun tests.
3. Use the bunshin resolver and listMachines; mengmotaMac is online and preferred. Fresh clone lcx/gate-softening in /tmp/lcx-fix13b-20260908/omo; overlay the worktree source differences, generator, and both regenerated tracked skill copies. Record file hashes. Install locked dependencies and build the plugin prerequisites before the complete remote plugin Node aggregate suite. Capture its complete log plus the final 200 lines and TEST_EXIT.
4. Remove the namespaced remote directory and capture the failed `ls -d` receipt. Update task-13-drift.log and task-13-doneclaim.json with observed results and the correct changed-file inventory. No commits, pushes, checkouts, stashes, shared-skills edits, or main-checkout edits.

## QA scope

This repair changes a generated-artifact equality assertion, not hook/installer behavior. The real surface is sync-skills generation, the resulting byte-identical artifact tree, and the flattened-cache generation test. No live model or real Codex-home access is needed.
