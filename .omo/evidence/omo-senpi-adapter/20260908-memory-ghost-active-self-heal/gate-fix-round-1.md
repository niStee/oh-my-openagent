gate-review fix round (2026-09-08): B1 missing-identity reclaim now attaches the on-disk ledger and only cleans a prelaunch whose ghost ownership is positively established (worktree differs from the ledger's); probe-when-possible preserves live launchers even without reservedAt. B2 run dirs attributed to a retired generation are skipped by the main loop whether or not the reclaim fires, so a retired ledger can never settle a live reservation through runId equality. B3 prelaunch cleanup is non-gating: unreadable/foreign/ambiguous artifacts are preserved and the failure surfaces as cleanupError on the reclaim result. Verified: worker 244/244, memory 1200/1200, tsgo clean, replica drive pass (this file refreshed).

Superseded: after rebasing onto dev d69e67401 the prelaunch-cleanup lane this note refers to was removed entirely (dev's policy: retired artifacts are never touched).
