# F1 plan-compliance — memorian-trigger-coverage

- Plan: `.omo/plans/memorian-trigger-coverage-late-judge.md`
- Range: `origin/dev` `192f5bfb7`…`HEAD` `f56a05368` (branch `feat/memorian-trigger-coverage`)
- PR: https://github.com/code-yeongyu/oh-my-openagent/pull/7895
- Scope audited: implementation todos **1, 2, 3, 4, 5, 7, 8** (all ticked). Todo 6 is unticked and is not treated as done.
- Method: committed diff + evidence logs. No `bun test` on this machine (task constraint). Independent read-only checks: `git diff origin/dev...HEAD`, the plan’s `awk` pure-LOC formula, forbidden-construct greps, `node packages/omo-senpi/plugin/scripts/build-extension.mjs --check`.

## Verdict

**APPROVE**

Every ticked todo’s agent-executable acceptance is present in the committed tree and in the named evidence logs. Todo 2’s original zero-accept `failed/deadline` pin is superseded at HEAD by todo 7 (`dropped/deadline`). No Must-NOT item is a product or test-hygiene blocker.

## Findings

| Item | Verdict | Exact evidence |
| --- | --- | --- |
| 1 Harvest planner tokens (eval `code`/`summary`, long `command`) | **PASS** | Assertions in `packages/omo-senpi/src/components/memory/recall-query-planner-tools.test.ts`: eval path `44:48` (`memorian-trigger.ts` + `memorian`); 300+ char bash `50:54` (`deep-file.ts`); secret `56:59` (`sk-abcdef` absent); summary `61:66` (`x.ts`); cap `68:71` (`<= 32`). Impl: `command`/`code` via `commandTexts` on the full string `recall-query-planner-tools.ts:67:71`; `isUsable` per token `11:13`/`41:45`; `summaryTexts` `51:61`/`72:74`; `MAX_TOKENS = 32` `6`; `TOOL_ARG_WINDOW = 8` `5`. Unknown keys still ignored `36:38`. RED then GREEN: `task-1-planner-tokens.log` sections 1–2 — RED `3 fail` (`memorian-trigger.ts` / `deep-file.ts` / `x.ts` missing) `RBT_RC=1`; GREEN `12 pass 0 fail` `RBT_RC=0`; `TSGO_RC=0`. Commit `2a0eedc2a`. |
| 2 Deadline salvage + 90 s `tool_call` | **PASS** (2b superseded by 7) | (a) salvage: `memorian-runner-completion.test.ts:124:138` `status: "nudged"` + `partial: true` + path, `deadlineMs: 50`, awaits launch promise. (c) compaction drop: `156:174` `dropped`/`compaction`. (d) `memorian-trigger.test.ts:45:61` `deadlineMs: 90_000`; settle has no deadline `89:93`. Impl: both deadline arms abort then salvage `memorian-judge-run.ts:100:107` and `110:116`; trigger `memorian-trigger.ts:155` `origin === "tool_call" ? { deadlineMs: 90_000 }`; settled default `memorian-runner.ts:43` `DEFAULT_DEADLINE_MS = 5 * 60_000`. Runner forwards `partial` after `validateNudges` + staleness `memorian-runner.ts:177:194`. RED/GREEN: `task-2-deadline-salvage.log` — RED `3 fail` including salvage + 90s pin, empty-deadline stayed green; GREEN `35 pass 0 fail` including `memorian-runner.test.ts`; `TSGO_RC=0`. Commit `05ff508f5`. |
| 3 `outcome.json` + 7 d / 30 d prune | **PASS** | Tests: completed outcome `memorian-run-retention.test.ts:45:65`; 8-day no-outcome removed `67:74`; 29 d nudged kept / 31 d removed `76:94`; 6-day failed kept `114:125`; newest-file 1 min skipped `127:132`; ENOENT tolerated `148:157`; throttle second call skipped `160:180`. Runner fake: `memorian-runner-completion.test.ts:180:194` `outcome.json` `status: "completed"`. Impl: `writeMemorianRunOutcome` mode `0o600` `memorian-run-retention.ts:61:66`; defaults `15:16`; `claimAndRemove` rename-then-remove `174:189`; every terminal return goes through `record()` `memorian-judge-run.ts:41:54`/`104:132`; settle prune `wiring-memorian.ts:48:56`/`77:83`. RED/GREEN: `task-3-run-retention.log` — RED missing module + outcome assertion `RBT_RC=1`; GREEN `30 pass 0 fail` `RBT_RC=0`; `tsgo` exit 0. Commit `c0c08abdd`. |
| 4 Live S6 + S3–S6 suite | **PASS** | Driver: `memorian-tool-boundary-e2e.mjs:11` `RUNNERS` includes `s6`; `43`/`55` `all` = s3–s6. GREEN: `green/driver-result.json` `"ok": true`, s3–s6 `"result": "PASS"`, each `"realSenpiUntouched": true`; s6 `judgeRequests: 2`, `candidatePaths: ["reference/kubernetes-rollouts.md"]`, checks `s6.judge-launched`/`s6.arg-harvested`/`s6.mid-turn-recall` all `ok: true`. Files: `green/s6-session.jsonl`, `green/mock-requests.jsonl`. RED: `red-s6/driver-result.json` `"ok": false`, s6 `"result": "FAIL"`, `judgeRequests: 0`, `s6.judge-launched`/`s6.arg-harvested` false (`recallRuns=0`); `realSenpiUntouched: true`. Log: `task-4-live-s6.log` RED then GREEN. Commit `dd4fcee3d`. Independent `--check` this audit: `node packages/omo-senpi/plugin/scripts/build-extension.mjs --check` → `CHECK_RC=0`; bundle contains `deadlineMs:9e4` and `toolTexts`. |
| 5 Docs + observability pins | **PASS** | AGENTS rows 3/25/26/27: intro prune `AGENTS.md:3`; harvest `code`/`summary` + per-token filter `25`; 90 s + salvage `26`; salvaged entry is normal `omo-memorian:nudged` `27`. Pins: salvage one nudged / zero gate `memorian-observability.test.ts:119:132`; dropped/deadline keeps a gate record `134:143`. Commit `f56a05368` source files: `AGENTS.md` + `memorian-observability.test.ts` (+ evidence log). GREEN: `task-5-docs-observability.log` `RBT_RC=0; 10 pass, 0 fail`; `tsgo` exit 0. |
| 7 Zero-accept deadline → `dropped` | **PASS** | Lifecycle pins flipped: `memorian-runner-lifecycle.test.ts:50`, `82`, `115` `{ status: "dropped", cause: "deadline" }`. Completion zero-accept: `memorian-runner-completion.test.ts:140:154` dropped + `status` not `nudged`. (ii) render undefined: `memorian-notice.test.ts:31:34`. (iii) gate record: `memorian-wiring.test.ts:29:41`. (iv) 30-day class: `memorian-run-retention.test.ts:94:112` + `retentionMs` `memorian-run-retention.ts:148:151`. Type: `MemorianGateFailureCause` `memorian-runner.ts:86` is `"session_create_failed" \| "child_failed" \| "launch_failed"` — no `"deadline"` (comment at `104` only). Both judge-run arms `memorian-judge-run.ts:107`/`116`. Copy: `memorian-notice.ts:91` dropped → `undefined`; `105:106` required sentences. RED/GREEN/mutation: `task-7-deadline-dropped.log` RED `4 fail` `RBT_RC=1`; GREEN `45 pass 0 fail`; mutation received `"failed"` expected `"dropped"` then GREEN again. `TSGO_RC=0`. Commit `0edab030c`. |
| 8 Tool-argument recall slots | **PASS** | (i) `planner.test.ts:110:116` queries contain `"rollout"`. (ii) stoplist `118:131`. (iii) newest-first `133:145`. (iv) characterization pins written first `87:108`. (v) wiring `recall-wiring.test.ts:270:283` extraTexts → `ROLLOUTS_PATH`. Path-before-command `recall-query-planner-tools.test.ts:20:26`. Impl: `planRecallQueries` `planner.ts:85:103` (`MAX_TOOL_SINGLE_TERMS = 2` `18`; user-only early return `91:93`); `collectFrom` `recall-wiring.ts:164:168` `toolTexts: [...extraTexts].reverse()`; `commandTexts` emits path words then command name `recall-query-planner-tools.ts:40:45`. RED/GREEN: `task-8-planner-tool-slots.log` characterization `16 pass` on unsynced planner; RED `4 fail` `RBT_RC=1`; GREEN `66 pass 0 fail`; `TSGO_SENPI=0` `TSGO_CORE=0`. Commit `2f6db9b8f`. |
| TDD: RED captured before GREEN | **PASS** | Todo 1 log §1 fail then §2 pass. Todo 2 log RED `3 fail` then GREEN `35 pass`. Todo 3 log missing module/outcome fail then GREEN. Todo 4 `red-s6/` then `green/` (`task-4-live-s6.log` order). Todo 7 flipped pins RED then impl GREEN then mutation. Todo 8 characterization GREEN → new tests RED → impl GREEN. Todo 5 captured an inverted-assertion RED then restored (pin on already-shipped behavior). |
| Pure-LOC ≤ 250 on every touched `.ts` | **PASS** | `awk '!/^[[:space:]]*$/ && !/^[[:space:]]*\/\//' <file> \| wc -l` at HEAD: planner.ts 133, planner.test.ts 163, judge-run.ts 134, notice.ts 95, notice.test.ts 50, observability.test.ts 179, run-retention.ts 215, run-retention.test.ts 180, runner-completion.test.ts 142, runner-lifecycle.test.ts 178, runner.ts 179, trigger.ts 222, trigger.test.ts 135, wiring.test.ts 50, planner-tools.ts 93, planner-tools.test.ts 68, recall-wiring.ts 186, recall-wiring.test.ts 217, wiring-memorian.ts 121. Max 222. None over 250. |
| Must-NOT: no new public config keys | **PASS** | `git diff origin/dev...HEAD -- packages/omo-config-core` empty. 90 s and 7/30 d are file-local constants (`memorian-trigger.ts:155`, `memorian-run-retention.ts:15:16`). |
| Must-NOT: no new deps | **PASS** | `git diff origin/dev...HEAD -- package.json bun.lock '**/package.json' '**/bun.lock'` empty. |
| Must-NOT: no `.skip`/`.only` | **PASS** | `git diff origin/dev...HEAD -- '*.test.ts' \| grep -E '^\+.*(describe\|test\|it)\.(skip\|only)'` → none. |
| Must-NOT: no fixed sleeps in new unit tests | **PASS** | Added test lines contain no `setTimeout` / `Bun.sleep` / `sleep(`. Salvage tests await the launch promise (`memorian-runner-completion.test.ts:132`). S6 driver uses subscribe-then-timeout (`memorian-tool-boundary-e2e-scenarios.mjs:206:214`) — not a unit test. |
| Must-NOT: no `any` / `as` (except `as const`/`satisfies`) / non-null `!` / `@ts-ignore` / `enum` on added `.ts` lines | **PASS** | `git diff origin/dev...HEAD -- '*.ts'` added-line scan: no hits. `as const satisfies` in `memorian-notice.test.ts:22:27` is the allowed exception. |
| Must-NOT: no tool RESULT / `write.content` / prompt-body harvest | **PASS** | `toolArgTexts` only reads `command`/`code`/`summary` plus existing PATH/ARRAY keys (`recall-query-planner-tools.ts:67:85`). Pre-existing unknown-key test still expects `[]` for `content` (`recall-query-planner-tools.test.ts:36:38`). Diff does not add `write.content`. |
| Must-NOT: delivery / drain / idle / task-child / facts untouched | **PASS** | `git diff --name-only origin/dev...HEAD` has no `memorian-delivery.ts`, `recall-drain.ts`, `idle-injection-coordinator.ts`, `in-process-memory-child.ts`. |
| Must-NOT: plugin extensions not hand-edited | **PASS** | `dd4fcee3d` updates `plugin/extensions/{omo.js,omo-task.js,memory-run-supervisor.mjs}` as regenerated bundles. This audit: `--check` exit 0. |
| Wave-1 remote suite (supporting log) | **NOTE** | `wave1-verify.md` (gitignored by `.gitignore:2` `.omo/*`; present on disk) recorded `1120 pass / 4 fail / 1 skip` at mid-wave HEAD `05ff508f5`. The 4 fails are `sandbox.test.ts` / `sandbox-absent-paths.test.ts` `bwrap: setting up uid map: Permission denied`, not memorian files. Ledger `wave1-verify-resolution` states the same 4 fails on origin/dev content. Not a ticked-todo acceptance. |

## Blockers

None.

## Notes

- **S6 tool is `eval`, not `bash`.** Plan what-to-do / success criterion 1 named a 300+ character `bash` `command`. The committed scenario uses `eval` + `code` because “the sandbox parent exposes eval, not bash” (`memorian-tool-boundary-e2e-scenarios.mjs:20:36`, `198:201`; `green/README.md`). Isolation still holds: prompt `please continue with the checklist` (`19`), token `rollout.md` only inside a 580-char tool argument, RED `judgeRequests=0`. Long-`command` harvest remains a unit pin (`recall-query-planner-tools.test.ts:50:54`).
- **S6 RED target is the beta.45 global plugin**, not a bundle built from `origin/dev` before todo 1 (`task-4-live-s6.log`). Failure mode matches the plan: fixture runs, zero judge launches.
- **Todo 2 acceptance (b)** required zero-accept `failed/deadline`. Todo 7 (user-direct, later) reclassified that outcome to `dropped/deadline`. HEAD follows todo 7. Plan Success criterion 2 still says `failed:deadline` and is stale relative to todo 7.
- **Todo 1 summary fixture** used English `"verify deadline salvage in packages/omo-senpi/x.ts"` rather than the plan’s Korean string; assertion still requires `x.ts`.
- **`memorian-notice.test.ts:38:47`** `toContain`s the full why-line sentences. That is a prose pin (Must-NOT). The same todo 7 required those exact strings in production (`memorian-notice.ts:105:106`); dropped/deadline rendering is the machine pin (`31:34`). Not treated as a blocker.
- **Todo 3 RED** was primarily `Cannot find module './memorian-run-retention'` plus the outcome.json assertion, not the plan’s “set max age to Infinity” mutation. Behavior is still tests-first.
- **Todo 5 RED** inverted the salvage gate-entry assertion rather than failing production-first. The committed pins match the plan.
- **Todo 8 stoplist** adds `pnpm`, `eval`, `read` beyond the plan’s list (`planner.ts:57:63`). Additive.
- **Todo 6** (package suite, PR-open evidence commit) is unticked; this audit does not claim `bun test --timeout 20000 packages/omo-senpi` 0-fail at HEAD.
- **This F1 pass did not re-run unit files on the ASCII box.** Plan F1 text asks for that; the task forbids `bun test` here. Remote logs above are the test evidence.
- Plugin `--check` this audit bundled in-memory and reported current; `git status` stayed clean (`feat/memorian-trigger-coverage...origin/feat/memorian-trigger-coverage`).
